apiVersion: v1
kind: Secret
metadata:
  name: job-be-secret
type: Opaque
data:
    POSTGRES_USER: { { .Values.database.username | b64enc | quote } }
    POSTGRES_PASSWORD: { { .Values.database.password | b64enc | quote } }
    POSTGRES_DB: { { .Values.database.dbname | b64enc | quote } }
